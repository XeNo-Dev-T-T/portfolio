[build]
  base = "."
  command = "pnpm install && pnpm --filter @workspace/portfolio run build"
  publish = "artifacts/portfolio/dist/public"

[build.environment]
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
